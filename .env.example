# ─────────────────────────────────────────────────────────────────────────────
# FozamiMail — environment configuration
# Copy this file to .env and fill in your values.
# .env is never committed to git (add it to .gitignore).
# ─────────────────────────────────────────────────────────────────────────────

# ── Gmail credentials ─────────────────────────────────────────────────────
# Your full Gmail address.
GMAIL_USER=your.address@gmail.com

# An App Password from your Google Account.
# How to create one:
#   1. Enable 2-Step Verification at myaccount.google.com/security
#   2. Go to myaccount.google.com/apppasswords
#   3. Select "Mail" + "Windows Computer" (or any device)
#   4. Copy the 16-character password and paste it here (spaces are fine).
GMAIL_APP_PASSWORD=xxxx xxxx xxxx xxxx

# ── Ollama ────────────────────────────────────────────────────────────────
# URL where Ollama is listening.  Default is fine for a local install.
OLLAMA_BASE_URL=http://localhost:11434

# Which model to use.  Must already be pulled: `ollama pull llama3.2`
# Good options: llama3.2, mistral, gemma2, phi3
OLLAMA_MODEL=mistral

# Seconds before an Ollama request is considered timed out.
OLLAMA_TIMEOUT=120

# ── Agent behaviour ───────────────────────────────────────────────────────
# How many seconds to wait between Gmail polls (default = 5 minutes).
POLL_INTERVAL_SECONDS=300

# Maximum characters of email body to send to the LLM.
# Keeps prompt size predictable; longer bodies are silently truncated.
MAX_BODY_CHARS=4000

# ── Storage ───────────────────────────────────────────────────────────────
# Path to the SQLite database file.
# Relative paths are resolved from the project root directory.
# DB_PATH=email_agent.db
